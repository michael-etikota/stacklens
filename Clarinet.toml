[project]
name = 'stacklens'
description = ''
authors = []
telemetry = false
cache_dir = './.cache'
requirements = []
[contracts.reward-simulator]
path = 'contracts/reward-simulator.clar'
clarity_version = 4
epoch = 'latest'

[contracts.stacking-analytics]
path = 'contracts/stacking-analytics.clar'
clarity_version = 4
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.lints]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
